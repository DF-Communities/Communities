({
	/*extractAddressFromEmbeddedVfPage : function(component, event, helper) {
		console.log('Extracting event params fired from embedded VF page');
        var street = event.getParam("street");
        var city = event.getParam("city");
        var county = event.getParam("county");
        var postcode = event.getParam("postcode");
        
        console.log("Extracted street as: " + street);
        console.log("Extracted city as: " + city);
        console.log("Extracted county as: " + county);
        console.log("Extracted postcode as: " + postcode);
        component.set("v.street", street);
        component.set("v.city", city);
        component.set("v.county", county);
        component.set("v.postcode", postcode);
	}*/
})